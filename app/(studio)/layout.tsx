import StudioLayout from "@/modules/studio/ui/layout/studio-layout";

const StudioLayoutPage = ({ children }: { children: React.ReactNode }) => {
  return <StudioLayout>{children}</StudioLayout>;
};

export default StudioLayoutPage;
